overwrite: true
schema: "src/services/graphql/schemas/*.graphql"
documents: null
generates:
  src/services/graphql/generated/API.ts:
    plugins:
      - "typescript"
      - "typescript-document-nodes"
